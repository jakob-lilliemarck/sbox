/*extern crate anyhow;
extern crate celery;

use anyhow::Result;

#[tokio::main]
async fn main() -> Result<()> {
    let my_app = sbox::tasks::create_app().await;
    my_app.consume_from(&["celery"]).await.unwrap();
    Ok(())
}*/
